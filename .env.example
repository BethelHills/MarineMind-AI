# Server-only — never prefix with VITE_ (that would expose it to the browser).
OPENAI_API_KEY=
PORT=3001

# Public backend URL for the AI Assistant page (safe for frontend).
VITE_API_URL=http://localhost:3001
